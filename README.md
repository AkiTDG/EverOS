EverOS

EverOS is a pseudo-Operating System that simulates how a CLI/Shell OS works. 
It contains multiple functions called as "features" that can perform tasks such as Calculator,
BMI Calculator, Temperature converter, and a Day-Time converter.

I made this system in-order to simulate how a CLI/Shell based Operating system actually works,
putting multiple features into it so that it wont function as a single purpose program.
